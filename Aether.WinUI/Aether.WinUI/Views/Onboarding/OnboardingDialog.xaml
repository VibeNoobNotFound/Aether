<ContentDialog
    x:Class="Aether.WinUI.Views.Onboarding.OnboardingDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Aether.WinUI.Controls"
    xmlns:models="using:Aether.WinUI.Models"
    mc:Ignorable="d"
    CornerRadius="24"
    Background="#FF0F0F13">

    <ContentDialog.Resources>
        <AcrylicBrush x:Key="DialogAcrylicBrush"
                      TintColor="#141A24"
                      TintOpacity="0.75"
                      FallbackColor="#FF0F0F13"/>
    </ContentDialog.Resources>

    <Grid MinWidth="960" MinHeight="680" MaxWidth="1400" MaxHeight="900" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF0B0B10" Offset="0"/>
                <GradientStop Color="#FF141A24" Offset="0.5"/>
                <GradientStop Color="#FF0A0A0F" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Aurora blobs -->
        <Canvas IsHitTestVisible="False">
            <Ellipse Width="540" Height="540" Fill="#334C9DFF" Canvas.Left="-140" Canvas.Top="-160"/>
            <Ellipse Width="460" Height="460" Fill="#333BB4C7" Canvas.Left="520" Canvas.Top="-140"/>
            <Ellipse Width="520" Height="520" Fill="#334B3AFF" Canvas.Left="260" Canvas.Top="260"/>
        </Canvas>

        <Grid RowDefinitions="Auto,*,Auto" Padding="24">
            <!-- Top Bar -->
            <Grid Grid.Row="0" ColumnDefinitions="*,Auto" Margin="0,0,0,12">
                <controls:ConnectionStatusBar/>
                <Button Grid.Column="1" Content="Skip" Click="Skip_Click" Background="Transparent" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </Grid>

            <!-- Slides -->
            <Border Grid.Row="1" Background="{StaticResource DialogAcrylicBrush}" CornerRadius="16" Padding="12">
                <FlipView x:Name="Slides" Background="Transparent" SelectionChanged="Slides_SelectionChanged">
                    <FlipViewItem>
                    <StackPanel Spacing="18" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE80F;" FontSize="64" Foreground="White"/>
                        <TextBlock Text="Welcome to Aether" FontSize="40" FontWeight="SemiBold" Foreground="White"/>
                        <TextBlock Text="Your unified game launcher" FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                    </FlipViewItem>

                    <FlipViewItem>
                    <StackPanel Spacing="16" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE7FC;" FontSize="60" Foreground="White"/>
                        <TextBlock Text="Unified Library" FontSize="32" FontWeight="SemiBold" Foreground="White"/>
                        <TextBlock Text="Scan Steam, Epic, and more into one place" FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextAlignment="Center"/>
                    </StackPanel>
                    </FlipViewItem>

                    <FlipViewItem>
                    <StackPanel Spacing="16" Padding="24">
                        <TextBlock Text="Plugins" FontSize="28" FontWeight="SemiBold" Foreground="White"/>
                        <TextBlock Text="Aether adapts to your library via plugins." FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <ItemsControl ItemsSource="{x:Bind Plugins, Mode=OneWay}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="models:PluginViewModel">
                                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,0,0,8">
                                        <FontIcon Glyph="&#xE8A5;" Foreground="White"/>
                                        <TextBlock Text="{x:Bind Name}" Foreground="White"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                    </FlipViewItem>

                    <FlipViewItem>
                    <StackPanel Spacing="16" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE9D2;" FontSize="60" Foreground="White"/>
                        <TextBlock Text="Insights" FontSize="32" FontWeight="SemiBold" Foreground="White"/>
                        <TextBlock Text="Track your playtime, genres, and favorites." FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextAlignment="Center"/>
                    </StackPanel>
                    </FlipViewItem>

                    <FlipViewItem>
                    <StackPanel Spacing="16" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE70F;" FontSize="60" Foreground="White"/>
                        <TextBlock Text="Customize Everything" FontSize="32" FontWeight="SemiBold" Foreground="White"/>
                        <TextBlock Text="Edit metadata and create collections your way." FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextAlignment="Center"/>
                    </StackPanel>
                    </FlipViewItem>

                    <FlipViewItem>
                    <StackPanel Spacing="16" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="Ready to start?" FontSize="32" FontWeight="SemiBold" Foreground="White"/>
                        <TextBlock Text="We will kick off your first scan." FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                    </FlipViewItem>
                </FlipView>
            </Border>

            <!-- Navigation -->
            <Grid Grid.Row="2" ColumnDefinitions="Auto,*,Auto" Margin="0,16,0,0">
                <Button x:Name="BackButton" Content="Back" Click="Back_Click" Padding="16,6"/>
                <TextBlock x:Name="SlideTextBlock" Grid.Column="1" Text="1 / 6" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button x:Name="NextButton" Grid.Column="2" Content="Next" Click="Next_Click" Padding="16,6"/>
            </Grid>
        </Grid>
    </Grid>
</ContentDialog>
