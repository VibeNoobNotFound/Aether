<Page
    x:Class="Aether.WinUI.Views.LibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Aether.WinUI.Controls"
    xmlns:data="using:Aether.WinUI.Models"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Library" Style="{StaticResource TitleTextBlockStyle}"/>
            <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                <Button Content="Refresh" Command="{x:Bind ViewModel.RefreshLibraryCommand}"/>
                <Button Content="Scan" Command="{x:Bind ViewModel.ScanLibraryCommand}"/>
            </StackPanel>
        </StackPanel>

        <!-- Game Grid -->
        <GridView Grid.Row="1"
                  ItemsSource="{x:Bind ViewModel.Games, Mode=OneWay}"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemClick="GridView_ItemClick">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="data:GameViewModel">
                    <controls:GameGridCard Game="{x:Bind}" Margin="0,0,12,12"/>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
