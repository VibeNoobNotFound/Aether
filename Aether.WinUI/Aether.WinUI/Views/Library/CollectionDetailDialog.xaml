<ContentDialog
    x:Class="Aether.WinUI.Views.Library.CollectionDetailDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Aether.WinUI.Views.Library"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Aether.WinUI.Models"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Title="Edit Collection"
    PrimaryButtonText="Save"
    Background="{ThemeResource AcrylicBackgroundFillColorBaseBrush}"
    CloseButtonText="Cancel"
    CornerRadius="24"
    DefaultButton="Primary">

    <Grid Width="500" Height="600">
        <StackPanel Spacing="16">
            <!-- Header Section -->
            <StackPanel Spacing="12">
                <TextBox x:Name="NameBox" Header="Name" PlaceholderText="Collection Name" 
                         Text="{x:Bind ViewModel.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         IsEnabled="{x:Bind ViewModel.CanEditName, Mode=OneWay}"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Icon" VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Click="IconPicker_Click">
                        <Grid Width="32" Height="32" CornerRadius="4" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                            <FontIcon Glyph="{x:Bind ViewModel.IconGlyph, Mode=OneWay}" FontSize="16"/>
                        </Grid>
                    </Button>
                </Grid>
                
                <MenuFlyoutSeparator/>
            </StackPanel>

            <!-- Game Selection (Only for Custom Collections) -->
            <Grid Visibility="{x:Bind ViewModel.IsCustomCollection, Mode=OneWay}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="Select Games" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,12"/>
                
                <ListView x:Name="GamesList" Grid.Row="1" Height="400" 
                          SelectionMode="Multiple"
                          BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" CornerRadius="4"
                          ItemsSource="{x:Bind ViewModel.AllGames, Mode=OneWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:GameViewModel">
                            <TextBlock Text="{x:Bind Title}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </StackPanel>
    </Grid>
</ContentDialog>
