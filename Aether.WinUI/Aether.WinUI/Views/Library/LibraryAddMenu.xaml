<ContentDialog
    x:Class="Aether.WinUI.Views.Library.LibraryAddMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Aether.WinUI.Views.Library"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Style="{StaticResource DefaultContentDialogStyle}"
    PrimaryButtonText="Cancel"
    IsPrimaryButtonEnabled="True">

    <Grid MinWidth="400" MaxWidth="600" MinHeight="300">
        <StackPanel Spacing="16">
            <ProgressBar x:Name="LoadingBar" IsIndeterminate="True" Visibility="Collapsed"/>
            
            <TextBlock x:Name="ErrorText" Foreground="Red" TextWrapping="Wrap" Visibility="Collapsed"/>
            
            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="500">
                <StackPanel x:Name="ContentPanel" Spacing="16" Padding="4">
                    <!-- Widgets Rendered Here -->
                </StackPanel>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</ContentDialog>
