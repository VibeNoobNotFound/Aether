<ContentDialog
    x:Class="Aether.WinUI.Views.Library.CollectionManagerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Aether.WinUI.Views.Library"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Aether.WinUI.Models"
    xmlns:converters="using:Aether.WinUI.Converters"
    mc:Ignorable="d"
    Title="Manage Collections"
    PrimaryButtonText="Done"
    DefaultButton="Primary">

    <ContentDialog.Resources>
        <converters:BoolToEyeIconConverter x:Key="BoolToEyeIconConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </ContentDialog.Resources>

    <Grid Width="500" Height="600">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar Background="Transparent" DefaultLabelPosition="Right">
            <AppBarButton Icon="Add" Label="New Collection" Click="NewCollection_Click"/>
        </CommandBar>

        <ListView x:Name="CollectionsList" Grid.Row="1" 
                  ItemsSource="{x:Bind ViewModel.Collections, Mode=OneWay}"
                  CanReorderItems="True" AllowDrop="True"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:CollectionViewModel">
                    <Grid Height="48" Padding="12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/> <!-- Drag Handle -->
                            <ColumnDefinition Width="Auto"/> <!-- Icon -->
                            <ColumnDefinition Width="*"/>    <!-- Name -->
                            <ColumnDefinition Width="Auto"/> <!-- Actions -->
                        </Grid.ColumnDefinitions>

                        <!-- Reorder Grip (Visual only) -->
                        <FontIcon Glyph="&#xE700;" Width="24" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                        <FontIcon Grid.Column="1" Glyph="{x:Bind Icon, Converter={StaticResource IconNameToGlyphConverter}}" Margin="12,0" FontSize="16"/>

                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Name}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="System" Visibility="{x:Bind IsSystem, Mode=OneWay}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="8">
                            <Button Click="Visibility_Click" Tag="{x:Bind}" Background="Transparent" BorderThickness="0">
                                <FontIcon Glyph="{x:Bind IsVisible, Mode=OneWay, Converter={StaticResource BoolToEyeIconConverter}}"/>
                            </Button>
                            
                            <Button Click="Edit_Click" Tag="{x:Bind}" Background="Transparent" BorderThickness="0">
                                <FontIcon Glyph="&#xE70F;"/> <!-- Pencil -->
                            </Button>

                            <Button Click="Delete_Click" Tag="{x:Bind}" Background="Transparent" BorderThickness="0" 
                                    Visibility="{x:Bind IsSystem, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}">
                                <FontIcon Glyph="&#xE74D;" Foreground="Red"/> <!-- Trash -->
                            </Button>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentDialog>
