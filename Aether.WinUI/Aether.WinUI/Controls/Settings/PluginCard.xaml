<UserControl
    x:Class="Aether.WinUI.Controls.Settings.PluginCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:models="using:Aether.WinUI.Models"
    mc:Ignorable="d">

    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
            CornerRadius="8" Padding="12" BorderThickness="1" 
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- icon -->
            <Border Width="48" Height="48" CornerRadius="8" Background="#2000FF00">
                <FontIcon Glyph="&#xE734;" FontSize="24" Foreground="Green"/>
            </Border>

            <!-- Info -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                <TextBlock Text="{x:Bind Plugin.Name, Mode=OneWay}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Border Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="4" Padding="4,2">
                        <TextBlock Text="{x:Bind Plugin.Version, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}"/>
                    </Border>
                    <TextBlock Text="{x:Bind Plugin.AuthorBinding, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
                
                <!-- Badges -->
                <ItemsControl ItemsSource="{x:Bind Plugin.Capabilities, Mode=OneWay}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" Spacing="4"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                             <Border Background="{ThemeResource AccentFillColorDefaultBrush}" CornerRadius="4" Padding="6,2">
                                <TextBlock Text="{x:Bind}" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <!-- Action -->
            <Button Grid.Column="2" VerticalAlignment="Center" Padding="8" Background="Transparent" BorderThickness="0">
                 <FontIcon Glyph="&#xE712;"/> <!-- More Options -->
                 <Button.Flyout>
                     <MenuFlyout>
                         <MenuFlyoutItem Text="Uninstall" Icon="Delete" Click="Uninstall_Click"/>
                     </MenuFlyout>
                 </Button.Flyout>
            </Button>
        </Grid>
    </Border>
</UserControl>
